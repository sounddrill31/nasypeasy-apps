services:
  linkding:
    image: docker.io/sissbruecker/linkding:latest
    container_name: nasypeasy-linkding
    ports:
      - "9091:9090"
    volumes:
      - linkding-data:/etc/linkding/data
    restart: unless-stopped

volumes:
  linkding-data:
